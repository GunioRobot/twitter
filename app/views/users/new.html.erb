<h1>Sign up</h1>

<p>Already a member? <%= link_to "Sign in", signin_path %></p>

<%= form_for(@user) do |f| %>
    <%= render "fields", :f => f %>
  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>
